package com.wiki4ai.model;

/**
 * Enum representing user roles in the Wiki4AI platform.
 * <p>
 * Used for role-based access control (RBAC).
 * - {@link #USER} Regular user with basic permissions.
 * - {@link #ADMIN} Administrator with full system access.
 */
public enum Role {
    USER,
    ADMIN
}
